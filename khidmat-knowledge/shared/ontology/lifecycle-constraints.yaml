version: "1.0.0"
domain: shared
file: shared/ontology/lifecycle-constraints.yaml
status: placeholder

# This file will contain descriptive lifecycle semantics, keyed by entity,
# shared across two or more domains. No shared lifecycle constraints are
# defined at current maturity.

lifecycle_constraints: {}
