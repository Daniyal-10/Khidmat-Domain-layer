version: "1.0.0"
domain: shared
file: shared/ontology/relationships.yaml
status: placeholder

# This file will contain relationship definitions shared across two or more domains.
# No cross-domain relationships are defined at current maturity.
#
# Candidate future entries:
#   - organisation employs person (volunteer-operations + programs)
#   - beneficiary enrolled_in program (beneficiary-lifecycle + programs)
#
# Do not add entries here until at least two active domains require the same relationship.

relationships: []