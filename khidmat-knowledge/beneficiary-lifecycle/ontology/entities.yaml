version: "1.0.0"
domain: beneficiary_lifecycle
file: ontology/entities.yaml
status: active

entities:
  - id: beneficiary_lifecycle
    label: Beneficiary Lifecycle
    description: >
      The authoritative lifecycle record representing a continuous, overarching journey of a beneficiary (Person or Household) within the Khidmat ecosystem.
    required: true

  - id: lifecycle_transition
    label: Lifecycle Transition
    description: >
      An immutable event representing a macro-state change within a beneficiary_lifecycle.
    required: false
