version: "1.1.0"
domain: consent-and-privacy
file: consent-and-privacy/ontology/semantic-constraints.yaml
status: active

constraints:
  - id: consent_expiration_logic
    description: >
      A consent's expiration date MUST be strictly greater than its granted date.
    rule: |
      consent.expiration_date > consent.granted_date
